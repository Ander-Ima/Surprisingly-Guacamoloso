#include <bits/stdc++.h>

using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    int N;
    cin >> N;
    vector<int> A(N + 1);
    vector<vector<int>> prefix(N + 1, vector<int>(N + 1, 0));
    for(int i = 1; i <= N; ++i){
        cin >> A[i];
    }
    for(int i = 1; i <= N; ++i){
        for(int j = 1; j <= N; ++j){
            
        }
    }

    return 0;
}