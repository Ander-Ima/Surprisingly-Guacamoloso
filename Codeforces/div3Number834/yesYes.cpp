#include <bits/stdc++.h>
using namespace std;


int main(){

    int casos;
    cin >> casos;

    string verdad = "Yes";

    while (casos--){
        
        string cadena;
        cin >> cadena;
        //vector <char> remake (cadena.begin(), cadena.end());
        
        auto pos = find(verdad.begin(), verdad.end(), cadena[0]);

        

    }
    


    return 0;
}